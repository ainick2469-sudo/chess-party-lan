services:
  - type: web
    name: chess-party-online
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build:hosted
    startCommand: node dist/server/index.cjs
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
